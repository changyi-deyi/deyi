@{
    WebManager.Model.MemberActDetail_Model view = new WebManager.Model.MemberActDetail_Model();
    Model.Manage_Model.MemberAct_Model model = new Model.Manage_Model.MemberAct_Model();
    if (Model != null)
    {
        view = Model;
        if (view.Data != null)
        {
            model = view.Data;
        }

    }
    string Domain = System.Configuration.ConfigurationManager.AppSettings["Domian"];
}

<!-- 配置文件 -->
<script type="text/javascript" src="/ueditor/ueditor.config.js"></script>
<!-- 编辑器源码文件 -->
<script type="text/javascript" src="/ueditor/ueditor.all.js"></script>
<script type="text/javascript">var txtRemark = UE.getEditor('txtRemark');</script>
<script src="/assets/js/ajaxfileupload.js"></script>
<script src="/Scripts/MemberAct.js"></script>
<!DOCTYPE html>
<!--页面主体-->
<div class="page-container">
    <!--左侧菜单-->
    @Html.Partial("Sidebar", 18)
    <!--左侧菜单 结束-->
    <!--右侧主体内容-->
    <div class="page-content">
        <div class="page-breadcrumbs">
            <ul class="breadcrumb">
                <li>
                    <a href="/Setting/MemberActList">活动设置</a>
                </li>
                <li class="active">详情</li>
            </ul>
        </div>
        <div class="page-body">
            <div id="popbg" class="modal-backdrop fade" style="display: none;"></div>
            <!-- Modal -->
            <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
                <div class="modal-dialog loading-icon" style="width: 400px; margin-top: 150px;">
                    <img src="/assets/img/loading.gif" />
                </div>
            </div>
            <div class="list-content">
                <div class="form-horizontal">
                    <div class="form-group">
                        <label class="col-md-2 control-label">图片</label>
                        <div class="col-md-9">
                            <div class="databox">
                                <div class="databox-left no-padding margin-right-20 " id="divPic">
                                    <img style="width: 65px; height: 65px;" id="img" src="@(string.IsNullOrEmpty(model.ImageURL) ? "/assets/img/Pic70.png": Domain+model.ImageURL)" f="@(model.ImageURL)">
                                    @if (!string.IsNullOrEmpty(model.ImageURL))
                                    {
                                        <a class="btn btn-danger btn-xs delete pull-right" style="position:absolute;right:0px;" onclick="deleteImage(this)"><i class="fa fa-trash-o"></i></a>
                                    }
                                </div>
                            </div>
                            <div class="form-group">
                                <input class="input-file pull-left" name="image_file" id="image_file" type="file" style="margin-left:15px;">
                                <a class="btn btn-sm green" onclick="UploadImage()"><i class="fa fa-upload"></i>上传图片</a>
                            </div>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-lg-2 control-label">名称</label>
                        <div class="col-lg-5">
                            <input class="form-control gh-w-100p" type="text" placeholder="请输入名称"  id="txtName" value="@(model.Name)"  />
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-lg-2 control-label">链接值</label>
                        <div class="col-lg-5">
                            <input class="form-control gh-w-100p" type="text" placeholder="请输入链接值"  id="txtLinkURL" value="@(model.LinkURL)" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-lg-2 control-label">参与等级</label>
                        <div class="col-lg-5">
                            <select class="select form-control gh-w-100p col-sm-12" id="ddlLevel">
                                <option value="0">全部等级</option>
                                @if (view.listLevel != null && view.listLevel.Count > 0)
                                {
                                    foreach (Model.Manage_Model.Level_Model item in view.listLevel)
                                    {
                                        <option value="@(item.LevelID)">@(item.Name)</option>

                                    }
                                }
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-lg-2 control-label">领取须知</label>
                        <div class="col-lg-10">
                            <script id="txtRemark" name="content" type="text/plain" style="width: 100%; height: 500px;">
                                @Html.Raw(model.Remark)
                            </script>
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-lg-7 text-right">
                            <a class=" btn-info btn-lg" onclick=" window.location.href=document.referrer;">返 回</a>
                            <a class=" btn-blue btn-lg"  onclick="operateMemberAct(@(view.ID))">保 存</a>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
<script>

    $(function () {
        $("#ddlLevel").val("@(model.LevelID)");
    });


    function UploadImage() {
        if ($("#image_file").val() == "") {
            alert("请选择图片");
            return;
        }

        $("#popbg").addClass("in").show();
        $("#myModal").addClass("in").show();
        $.ajaxFileUpload
            (
            {
                url: '/File/FileUpload', //用于文件上传的服务器端请求地址
                data: {

                },
                secureuri: false, //一般设置为false
                fileElementId: 'image_file', //文件上传空间的id属性  <input type="file" id="file" name="file" />
                dataType: 'json', //返回值类型 一般设置为json
                success: function (data)  //服务器成功响应处理函数
                {
                    if (data.Code == "1") {
                        showImage(data.Data, data.FileName);
                        alert("上传成功");
                    } else {
                        alert("上传失败");
                    }
                    $("#popbg").removeClass("in").hide();
                    $("#myModal").removeClass("in").hide();
                },
                error: function (data, status, e)//服务器响应失败处理函数
                {
                    alert(e);
                    $("#popbg").removeClass("in").hide();
                    $("#myModal").removeClass("in").hide();
                }
            }
            );
    }

    function showImage(url, file_name) {
        $("#img").attr("src", url);
        $("#img").attr("f", file_name);
        if ($("#divPic a").size() == 0) {
            $("#divPic").append('<a class="btn btn-danger btn-xs delete pull-right" style="position:absolute;right:0px;" onclick="deleteImage(this)" ><i class="fa fa-trash-o"></i></a>');
        }
    }

    function deleteImage(e) {
        $("#img").attr("f", "");
        $("#img").attr("src", "/assets/img/Pic70.png");
        $(e).remove();
    }
</script>
